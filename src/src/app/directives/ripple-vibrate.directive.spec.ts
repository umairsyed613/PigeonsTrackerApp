import { RippleVibrateDirective } from './ripple-vibrate.directive';

describe('RippleVibrateDirective', () => {
  it('should create an instance', () => {
    const directive = new RippleVibrateDirective();
    expect(directive).toBeTruthy();
  });
});
